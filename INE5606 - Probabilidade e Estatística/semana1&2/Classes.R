# Tipos de classe no R

# nnumerico

numeros <- c(1,2,3)
numeros
class(numeros)
length(numeros)

# inteiro
Numeros = 1:6
Numeros

#caracteres

'a'
letras <- c('a', 'b', 'c')
alfabeto
silabas = c('ba', 'ca', 'sa')

# Logico 

logico <- c(TRUE, FALSE, FALSE, TRUE, FALSE)

ls()

rm(delta)
ls()

juntos <- c(alfabeto, letras, silabas)
juntosConcat <- data.frame(alfabeto, logico, numeros)

juntosConcat
